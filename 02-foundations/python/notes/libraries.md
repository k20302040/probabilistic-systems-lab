## A python library is a set of code previously written by either you or someone else that you can import into your current project.
An example would be the "random" module.

------------------
#### tells python you want to use this library
import random

#### random.choice() accesses a specific function inside of random. 
coin = random.choice(["heads", "tails"])
print(coin)
