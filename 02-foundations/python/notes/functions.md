## A function is a reuseable piece of code that performs a task. Functions allow you to write a piece of code once and then reuse it an infinite number of times without rewriting the whole thing. You can also pass in parameters.

### Example:
    print()
    int()
    
    def hello():
      iluhjkbnlk.jn
      kjhgblkjhbn
    
    hello()
